global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "barbershop-api"

    metrics_path: "/q/metrics"

    static_configs:
      - targets:
          - "host.docker.internal:8080"

  - job_name: "postgres"

    static_configs:
      - targets:
          - "postgres-exporter:9187"
